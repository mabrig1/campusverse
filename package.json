{
  "name": "campusverse-monorepo",
  "version": "1.0.0",
  "description": "CampusVerse - Trusted Student Marketplace and Social Network",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm start --prefix backend",
    "install:all": "npm install --workspaces --if-present"
  }
}
